{
  "name": "assignment",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k yarn:firebase vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "firebase": "firebase emulators:start --only auth,firestore,functions --import=./offline.seed --export-on-exit --project it1552-assignment"
  },
  "dependencies": {
    "firebase": "^9.9.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.3.0",
    "zustand": "^4.0.0-rc.1"
  },
  "devDependencies": {
    "@types/react": "^18.0.15",
    "@types/react-dom": "^18.0.6",
    "@vitejs/plugin-react": "^2.0.0",
    "concurrently": "^7.2.2",
    "typescript": "^4.6.4",
    "vite": "^3.0.0"
  }
}
